name: Golden Files

on:
  pull_request:

jobs:
  golden-files:
    name: Check golden files
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.27.1'
          check-latest: true
          cache: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Download dependencies
        run: go mod download

      - name: Install tools
        run: |
          go install github.com/a-h/templ/cmd/templ
          go install github.com/segmentio/golines
          go install golang.org/x/tools/cmd/goimports

      - name: Update golden files
        run: just update-golden-all

      - name: Check for golden file changes
        run: |
          golden_paths='e2e/testdata/golden generator/testdata/golden'
          if [ -n "$(git status --porcelain -- ${golden_paths})" ]; then
            echo "Golden files are out of date. Run 'just update-golden-all' locally and commit the changes."
            git status --short -- ${golden_paths}
            git diff -- ${golden_paths}
            exit 1
          fi
