-- narsilc queries for Product

-- name: GetProduct :one
SELECT *
FROM products
WHERE id = $1
LIMIT 1;

-- name: ListProducts :many
-- @order id
SELECT *
FROM products;

-- name: CountProducts :one
SELECT count(*)
FROM products;
