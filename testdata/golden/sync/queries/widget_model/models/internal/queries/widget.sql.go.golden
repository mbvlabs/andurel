// Code generated by narsilc. DO NOT EDIT.
// source: widget.sql

package queries

import (
	"context"
	"fmt"
	"strings"

	"github.com/jackc/pgx/v5/pgtype"
	"github.com/mbvlabs/narsilc"
	"uuid"
)

const countWidgets = `-- name: CountWidgets :one
SELECT count(*)
FROM widgets
`

func (q *Queries) CountWidgets(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countWidgets)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createWidget = `-- name: CreateWidget :one
INSERT INTO widgets (
	id,
	name,
	quantity,
	active,
	created_at,
	updated_at
) VALUES (
	$1,
	$2,
	$3,
	$4,
	$5,
	$6
)
RETURNING id, name, quantity, active, created_at, updated_at
`

type CreateWidgetParams struct {
	ID        uuid.UUID
	Name      string
	Quantity  int32
	Active    bool
	CreatedAt pgtype.Timestamptz
	UpdatedAt pgtype.Timestamptz
}

func (q *Queries) CreateWidget[T any](ctx context.Context, arg CreateWidgetParams) (T, error) {
	row := q.db.QueryRow(ctx, createWidget,
		arg.ID,
		arg.Name,
		arg.Quantity,
		arg.Active,
		arg.CreatedAt,
		arg.UpdatedAt,
	)
	return narsilc.Scan[T](row, []string{"id", "name", "quantity", "active", "created_at", "updated_at"})
}

const deleteWidget = `-- name: DeleteWidget :exec
DELETE FROM widgets
WHERE id = $1
`

func (q *Queries) DeleteWidget(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteWidget, id)
	return err
}

const getWidget = `-- name: GetWidget :one

SELECT id, name, quantity, active, created_at, updated_at
FROM widgets
WHERE id = $1
LIMIT 1
`

// narsilc queries for Widget
func (q *Queries) GetWidget[T any](ctx context.Context, id uuid.UUID) (T, error) {
	row := q.db.QueryRow(ctx, getWidget, id)
	return narsilc.Scan[T](row, []string{"id", "name", "quantity", "active", "created_at", "updated_at"})
}

const listWidgets = `-- name: ListWidgets :many
SELECT id, name, quantity, active, created_at, updated_at
FROM widgets
`

func (q *Queries) ListWidgets[T any](ctx context.Context) *ListWidgetsBuilder[T] {
	return &ListWidgetsBuilder[T]{
		q:        q,
		ctx:      ctx,
		args:     []any{},
		hasWhere: false,
		dollar:   true,
	}
}

type ListWidgetsBuilder[T any] struct {
	q        *Queries
	ctx      context.Context
	args     []any
	conds    []string
	order    string
	limit    *int32
	offset   *int32
	hasWhere bool
	dollar   bool
}

func (b *ListWidgetsBuilder[T]) OrderByIDAsc() *ListWidgetsBuilder[T] {
	b.order = `id ASC`
	return b
}

func (b *ListWidgetsBuilder[T]) OrderByIDDesc() *ListWidgetsBuilder[T] {
	b.order = `id DESC`
	return b
}

func (b *ListWidgetsBuilder[T]) Limit(n int32) *ListWidgetsBuilder[T] {
	b.limit = &n
	return b
}

func (b *ListWidgetsBuilder[T]) Offset(n int32) *ListWidgetsBuilder[T] {
	b.offset = &n
	return b
}

func (b *ListWidgetsBuilder[T]) build() string {
	query := listWidgets
	if len(b.conds) > 0 {
		if b.hasWhere {
			query += " AND " + strings.Join(b.conds, " AND ")
		} else {
			query += " WHERE " + strings.Join(b.conds, " AND ")
		}
	}
	if b.order != "" {
		query += " ORDER BY " + b.order
	}
	if b.limit != nil {
		b.args = append(b.args, *b.limit)
		if b.dollar {
			query += fmt.Sprintf(" LIMIT $%d", len(b.args))
		} else {
			query += " LIMIT ?"
		}
	}
	if b.offset != nil {
		b.args = append(b.args, *b.offset)
		if b.dollar {
			query += fmt.Sprintf(" OFFSET $%d", len(b.args))
		} else {
			query += " OFFSET ?"
		}
	}
	return query
}

func (b *ListWidgetsBuilder[T]) All() ([]T, error) {
	query := b.build()
	rows, err := b.q.db.Query(b.ctx, query, b.args...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []T
	for rows.Next() {
		item, err := narsilc.Scan[T](rows, []string{"id", "name", "quantity", "active", "created_at", "updated_at"})
		if err != nil {
			return nil, err
		}
		items = append(items, item)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateWidget = `-- name: UpdateWidget :one
UPDATE widgets
SET
	name = $2,
	quantity = $3,
	active = $4,
	updated_at = $5
WHERE id = $1
RETURNING id, name, quantity, active, created_at, updated_at
`

type UpdateWidgetParams struct {
	ID        uuid.UUID
	Name      string
	Quantity  int32
	Active    bool
	UpdatedAt pgtype.Timestamptz
}

func (q *Queries) UpdateWidget[T any](ctx context.Context, arg UpdateWidgetParams) (T, error) {
	row := q.db.QueryRow(ctx, updateWidget,
		arg.ID,
		arg.Name,
		arg.Quantity,
		arg.Active,
		arg.UpdatedAt,
	)
	return narsilc.Scan[T](row, []string{"id", "name", "quantity", "active", "created_at", "updated_at"})
}

const upsertWidget = `-- name: UpsertWidget :one
INSERT INTO widgets (
	id,
	name,
	quantity,
	active,
	created_at,
	updated_at
) VALUES (
	$1,
	$2,
	$3,
	$4,
	$5,
	$6
)
ON CONFLICT (id) DO UPDATE SET
	name = excluded.name,
	quantity = excluded.quantity,
	active = excluded.active,
	updated_at = excluded.updated_at
RETURNING id, name, quantity, active, created_at, updated_at
`

type UpsertWidgetParams struct {
	ID        uuid.UUID
	Name      string
	Quantity  int32
	Active    bool
	CreatedAt pgtype.Timestamptz
	UpdatedAt pgtype.Timestamptz
}

func (q *Queries) UpsertWidget[T any](ctx context.Context, arg UpsertWidgetParams) (T, error) {
	row := q.db.QueryRow(ctx, upsertWidget,
		arg.ID,
		arg.Name,
		arg.Quantity,
		arg.Active,
		arg.CreatedAt,
		arg.UpdatedAt,
	)
	return narsilc.Scan[T](row, []string{"id", "name", "quantity", "active", "created_at", "updated_at"})
}
