<script setup lang="ts">
import { Link, useForm } from '@inertiajs/vue3'
import Layout from '../../Layouts/Layout.vue'

defineProps<{ errors?: Record<string, string> }>()

const form = useForm({
  email: '',
  password: '',
  confirmPassword: '',
})
</script>

<template>
  <Layout>
    <section class="w-full max-w-md rounded-lg border border-cyan-400/25 bg-slate-900 shadow-sm shadow-cyan-950/30">
      <div class="p-8 pb-0">
        <h1 class="text-xl font-semibold text-slate-100">Create an account</h1>
        <p class="mt-1 text-sm text-slate-400">Enter your details below to create your account</p>
      </div>
      <div class="p-8">
        <form class="space-y-5" @submit.prevent="form.post('/users')">
          <div class="space-y-1">
            <label class="text-sm font-medium text-slate-200" for="email">Email</label>
            <input id="email" v-model="form.email" type="email" class="flex h-9 w-full rounded border border-cyan-400/25 bg-slate-950 px-3 py-1 text-sm text-slate-100 shadow-inner shadow-cyan-950/30 focus:border-cyan-400 focus:outline-none focus:ring-2 focus:ring-cyan-400/40" required>
            <p v-if="errors?.email" class="text-sm font-medium text-red-400">{{ errors.email }}</p>
          </div>
          <div class="space-y-1">
            <label class="text-sm font-medium text-slate-200" for="password">Password</label>
            <input id="password" v-model="form.password" type="password" class="flex h-9 w-full rounded border border-cyan-400/25 bg-slate-950 px-3 py-1 text-sm text-slate-100 shadow-inner shadow-cyan-950/30 focus:border-cyan-400 focus:outline-none focus:ring-2 focus:ring-cyan-400/40" required>
            <p v-if="errors?.password" class="text-sm font-medium text-red-400">{{ errors.password }}</p>
          </div>
          <div class="space-y-1">
            <label class="text-sm font-medium text-slate-200" for="confirmPassword">Confirm Password</label>
            <input id="confirmPassword" v-model="form.confirmPassword" type="password" class="flex h-9 w-full rounded border border-cyan-400/25 bg-slate-950 px-3 py-1 text-sm text-slate-100 shadow-inner shadow-cyan-950/30 focus:border-cyan-400 focus:outline-none focus:ring-2 focus:ring-cyan-400/40" required>
            <p v-if="errors?.confirmPassword" class="text-sm font-medium text-red-400">{{ errors.confirmPassword }}</p>
          </div>
          <button type="submit" :disabled="form.processing" class="inline-flex w-full items-center justify-center rounded bg-cyan-400 px-4 py-2 text-sm font-medium text-slate-950 shadow-sm shadow-cyan-950/30 hover:bg-cyan-300 disabled:opacity-60">{{ form.processing ? 'Loading' : 'Sign Up' }}</button>
        </form>
        <p class="mt-6 text-center text-sm text-slate-400">Already have an account? <Link class="text-slate-300 hover:text-slate-100 hover:underline" href="/users/sign-in">Login</Link></p>
      </div>
    </section>
  </Layout>
</template>
