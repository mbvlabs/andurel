// Package inertia implements the Inertia v3 protocol for Echo and templ.
// Code generated by andurel {{.FrameworkVersion}}; DO NOT EDIT.
package inertia

import "fmt"

// ErrorKind identifies the operation that failed without exposing prop values.
type ErrorKind string

const (
	ErrorProtocol ErrorKind = "protocol"
	ErrorProps    ErrorKind = "props"
	ErrorRoot     ErrorKind = "root"
	ErrorSSR      ErrorKind = "ssr"
)

// Error adds safe request and page context to an Inertia failure.
type Error struct {
	Kind      ErrorKind
	Operation string
	Component string
	Method    string
	URL       string
	PropPath  string
	Err       error
}

func (e *Error) Error() string {
	s := "inertia " + string(e.Kind)
	if e.Operation != "" {
		s += " " + e.Operation
	}
	if e.Component != "" {
		s += " for component " + e.Component
	}
	if e.PropPath != "" {
		s += " at prop " + e.PropPath
	}
	if e.Err != nil {
		s += ": " + e.Err.Error()
	}
	return s
}

func (e *Error) Unwrap() error { return e.Err }

func protocolError(operation, message string) error {
	return &Error{Kind: ErrorProtocol, Operation: operation, Err: fmt.Errorf("%s", message)}
}
