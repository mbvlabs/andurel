<!-- Use for time-of-day capture when the browser-native picker is acceptable. -->
<section class="space-y-4 rounded-box border border-base-300 bg-base-100 p-6">
  <header class="space-y-1">
    <h2 class="text-xl font-semibold">Time Input</h2>
    <p class="text-sm text-base-content/70">Time field using the shared input styling.</p>
  </header>

  <div class="field">
    <label for="example-time" class="field-label">Deployment window</label>
    <input id="example-time" type="time" class="input" />
  </div>
</section>
