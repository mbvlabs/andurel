package routes

import "net/http"

const (
	APIRoutePrefix = "/api"
	apiNamePrefix  = "api"
)

var Health = Route{
	Name:             apiNamePrefix + ".health",
	Path:             APIRoutePrefix + "/health",
	Method:           http.MethodGet,
	Controller:       "API",
	ControllerMethod: "Health",
}
