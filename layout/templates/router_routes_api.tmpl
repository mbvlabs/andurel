package routes

import (
	"{{.ModuleName}}/internal/andurel"
)

const (
	APIRoutePrefix = "/api"
	apiNamePrefix  = "api"
)

var Health = andurel.NewSimpleRoute(
	APIRoutePrefix+"/health",
	"health",
	apiNamePrefix,
)
