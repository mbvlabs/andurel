package views

import (
	"net/http"
	"{{.ModuleName}}/internal/hypermedia"
	"{{.ModuleName}}/router/routes"
)

templ RegistrationForm() {
	@base() {
		<main class="auth-main flex items-center justify-center">
			<div class="auth-shell">
				<div class="auth-card">
					<form class="space-y-5" data-indicator:submitting data-on:submit={ "!$submitting && " + hypermedia.DataAction(http.MethodPost, routes.RegistrationCreate.URL()) }>
						<fieldset data-attr:disabled="$submitting">
							<div class="space-y-1">
								<h2 class="auth-title">Create an account</h2>
								<p class="auth-subtitle">Enter your details below to create your account</p>
							</div>
							<div class="auth-fields">
								<div class="auth-field">
									<label class="field-label" for="email">Email</label>
									<input id="email" type="email" class="field-input" data-bind="email" required/>
								</div>
								<div class="auth-field">
									<label class="field-label" for="password">Password</label>
									<input id="password" type="password" class="field-input" data-bind="password" required/>
								</div>
								<div class="auth-field">
									<label class="field-label" for="confirmPassword">Confirm Password</label>
									<input id="confirmPassword" type="password" class="field-input" data-bind="confirmPassword" required/>
								</div>
							</div>
							<button type="submit" class="auth-submit">
								<span data-show="!$submitting">Sign Up</span>
								<span data-show="$submitting">Loading</span>
							</button>
						</fieldset>
					</form>
					<p class="auth-links">
						Already have an account? <a class="inline-link" href={ routes.SessionNew.URL() }>Login</a>
					</p>
				</div>
			</div>
		</main>
	}
}
