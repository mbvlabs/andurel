<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title inertia>{{ .appName }}</title>
    {{ .inertiaHead }}
    {{ .viteHead }}
</head>
<body>
    {{ .inertia }}
    {{ .viteBody }}
</body>
</html>
