<!-- Use for date capture when the browser-native picker is acceptable. -->
<section class="space-y-4 rounded-box border border-base-300 bg-base-100 p-6">
  <header class="space-y-1">
    <h2 class="text-xl font-semibold">Date Input</h2>
    <p class="text-sm text-base-content/70">Date field using the shared input styling.</p>
  </header>

  <div class="field">
    <label for="example-date" class="field-label">Release date</label>
    <input id="example-date" type="date" class="input" />
  </div>
</section>
