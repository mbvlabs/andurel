package controllers

import (
	"{{.ModuleName}}/internal/andurel"
	"net/http"

	"github.com/labstack/echo/v4"
)

type API struct {
	db andurel.Database
}

func NewAPI(db andurel.Database) API {
	return API{db}
}

func (a API) Health(ctx echo.Context) error {
	return ctx.JSON(http.StatusOK, "app is healthy and running")
}
