// Code generated by andurel. DO NOT EDIT.
// This file contains constructor functions for SQLC parameters.
// These functions are automatically updated during schema refresh operations.

package db

{{ if eq .Database "postgresql" }}
import (
	"database/sql"

	"github.com/google/uuid"
)
{{ else if eq .Database "sqlite" }}
import (
	"database/sql"

	"github.com/google/uuid"
)
{{ end }}

// Constructor functions for SQLC parameters - these get updated during refresh
// to make schema changes compiler-enforced and visible

func CreateInsertPaddleProductParams(
	id uuid.UUID,
	paddleProductID string,
	paddlePriceID string,
	name string,
	description sql.NullString,
	priceAmount string,
	priceCurrency string,
	imageURL sql.NullString,
	isActive bool,
) InsertPaddleProductParams {
{{- if eq .Database "postgresql" }}
	return InsertPaddleProductParams{
		ID:              id,
		PaddleProductID: paddleProductID,
		PaddlePriceID:   paddlePriceID,
		Name:            name,
		Description:     description,
		PriceAmount:     priceAmount,
		PriceCurrency:   priceCurrency,
		ImageUrl:        imageURL,
		IsActive:        isActive,
	}
{{- else if eq .Database "sqlite" }}
	var sqliteIsActive int64
	if isActive {
		sqliteIsActive = 1
	}

	return InsertPaddleProductParams{
		ID:              id.String(),
		PaddleProductID: paddleProductID,
		PaddlePriceID:   paddlePriceID,
		Name:            name,
		Description:     description,
		PriceAmount:     priceAmount,
		PriceCurrency:   priceCurrency,
		ImageUrl:        imageURL,
		IsActive:        sqliteIsActive,
	}
{{- end }}
}

func CreateUpdatePaddleProductParams(
	id uuid.UUID,
	name string,
	description sql.NullString,
	priceAmount string,
	priceCurrency string,
	imageURL sql.NullString,
	isActive bool,
) UpdatePaddleProductParams {
{{- if eq .Database "postgresql" }}
	return UpdatePaddleProductParams{
		ID:            id,
		Name:          name,
		Description:   description,
		PriceAmount:   priceAmount,
		PriceCurrency: priceCurrency,
		ImageUrl:      imageURL,
		IsActive:      isActive,
	}
{{- else if eq .Database "sqlite" }}
	var sqliteIsActive int64
	if isActive {
		sqliteIsActive = 1
	}

	return UpdatePaddleProductParams{
		ID:            id.String(),
		Name:          name,
		Description:   description,
		PriceAmount:   priceAmount,
		PriceCurrency: priceCurrency,
		ImageUrl:      imageURL,
		IsActive:      sqliteIsActive,
	}
{{- end }}
}
