package main

import (
	"testapp/controllers"
	"testapp/router"
)

func setupControllers(db interface{}, r *router.Router) error {
	documents := controllers.NewDocuments(db)
	if err := r.RegisterDocumentRoutes(documents); err != nil {
		return err
	}

	// andurel:controller-registration-point
	return nil
}
