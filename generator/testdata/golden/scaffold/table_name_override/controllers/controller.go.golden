package controllers

import (
	"testapp/router"

	"go.uber.org/fx"
)

var constructors = fx.Provide(
	NewStudentFeedback,
)

var Module = fx.Module(
	"controllers",
	constructors,
	fx.Invoke(func(r *router.Router, c StudentFeedback) error {
		return c.RegisterRoutes(r)
	}),
)
