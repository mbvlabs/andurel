package main

import (
	"testapp/controllers"
	"testapp/router"
)

func setupControllers(db interface{}, r *router.Router) error {
	widgets := controllers.NewWidgets(db)
	if err := r.RegisterWidgetRoutes(widgets); err != nil {
		return err
	}

	// andurel:controller-registration-point
	return nil
}
