package routes

import (
	"{{.ModulePath}}/internal/routing"
)

const {{.ResourceName}}Prefix = "/{{.PluralName}}"

var {{.ResourceName}}Index = routing.NewSimpleRoute(
	"/",
	"{{.PluralName}}.index",
	{{.ResourceName}}Prefix,
)

var {{.ResourceName}}Show = routing.NewRouteWithID(
	"/:id",
	"{{.PluralName}}.show",
	{{.ResourceName}}Prefix,
)

var {{.ResourceName}}New = routing.NewSimpleRoute(
	"/new",
	"{{.PluralName}}.new",
	{{.ResourceName}}Prefix,
)

var {{.ResourceName}}Create = routing.NewSimpleRoute(
	"/",
	"{{.PluralName}}.create",
	{{.ResourceName}}Prefix,
)

var {{.ResourceName}}Edit = routing.NewRouteWithID(
	"/:id/edit",
	"{{.PluralName}}.edit",
	{{.ResourceName}}Prefix,
)

var {{.ResourceName}}Update = routing.NewRouteWithID(
	"/:id",
	"{{.PluralName}}.update",
	{{.ResourceName}}Prefix,
)

var {{.ResourceName}}Destroy = routing.NewRouteWithID(
	"/:id",
	"{{.PluralName}}.destroy",
	{{.ResourceName}}Prefix,
)
