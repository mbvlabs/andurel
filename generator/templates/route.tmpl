package routes

import (
	"{{.ModulePath}}/internal/andurel"
)

const (
	{{.PluralName | ToCamelCase}}RoutePrefix = "/{{.PluralName}}"
	{{.PluralName | ToCamelCase}}NamePrefix  = "{{.PluralName}}"
)

var {{.ResourceName}}Index = andurel.NewSimpleRoute(
	{{.PluralName | ToCamelCase}}RoutePrefix,
	"index",
	{{.PluralName | ToCamelCase}}NamePrefix,
)

var {{.ResourceName}}Show = andurel.NewRouteWithID(
	{{.PluralName | ToCamelCase}}RoutePrefix+"/:id",
	"show",
	{{.PluralName | ToCamelCase}}NamePrefix,
)

var {{.ResourceName}}New = andurel.NewSimpleRoute(
	{{.PluralName | ToCamelCase}}RoutePrefix+"/new",
	"new",
	{{.PluralName | ToCamelCase}}NamePrefix,
)

var {{.ResourceName}}Create = andurel.NewSimpleRoute(
	{{.PluralName | ToCamelCase}}RoutePrefix,
	"create",
	{{.PluralName | ToCamelCase}}NamePrefix,
)

var {{.ResourceName}}Edit = andurel.NewRouteWithID(
	{{.PluralName | ToCamelCase}}RoutePrefix+"/:id/edit",
	"edit",
	{{.PluralName | ToCamelCase}}NamePrefix,
)

var {{.ResourceName}}Update = andurel.NewRouteWithID(
	{{.PluralName | ToCamelCase}}RoutePrefix+"/:id",
	"update",
	{{.PluralName | ToCamelCase}}NamePrefix,
)

var {{.ResourceName}}Destroy = andurel.NewRouteWithID(
	{{.PluralName | ToCamelCase}}RoutePrefix+"/:id",
	"destroy",
	{{.PluralName | ToCamelCase}}NamePrefix,
)
