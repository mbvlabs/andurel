package routes

import "net/http"

const (
	{{.PluralName | ToCamelCase}}RoutePrefix = "/{{.PluralName}}"
	{{.PluralName | ToCamelCase}}NamePrefix  = "{{.PluralName}}"
)

var {{.PluralName | ToCamelCase}}Group = NewRouteGroup({{.PluralName | ToCamelCase}}NamePrefix, {{.PluralName | ToCamelCase}}RoutePrefix)

var {{.ResourceName}}Index = {{.PluralName | ToCamelCase}}Group.Route("index").
	SetMethod(http.MethodGet).
	SetCtrl("{{.ResourceName}}s", "Index").
	Register()

var {{.ResourceName}}Show = {{.PluralName | ToCamelCase}}Group.Route("show").
	SetPath("/:id").
	SetMethod(http.MethodGet).
	SetCtrl("{{.ResourceName}}s", "Show").
	RegisterWithID()

var {{.ResourceName}}New = {{.PluralName | ToCamelCase}}Group.Route("new").
	SetPath("/new").
	SetMethod(http.MethodGet).
	SetCtrl("{{.ResourceName}}s", "New").
	Register()

var {{.ResourceName}}Create = {{.PluralName | ToCamelCase}}Group.Route("create").
	SetMethod(http.MethodPost).
	SetCtrl("{{.ResourceName}}s", "Create").
	Register()

var {{.ResourceName}}Edit = {{.PluralName | ToCamelCase}}Group.Route("edit").
	SetPath("/:id/edit").
	SetMethod(http.MethodGet).
	SetCtrl("{{.ResourceName}}s", "Edit").
	RegisterWithID()

var {{.ResourceName}}Update = {{.PluralName | ToCamelCase}}Group.Route("update").
	SetPath("/:id").
	SetMethod(http.MethodPut).
	SetCtrl("{{.ResourceName}}s", "Update").
	RegisterWithID()

var {{.ResourceName}}Destroy = {{.PluralName | ToCamelCase}}Group.Route("destroy").
	SetPath("/:id").
	SetMethod(http.MethodDelete).
	SetCtrl("{{.ResourceName}}s", "Destroy").
	RegisterWithID()
