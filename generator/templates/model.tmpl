package models

// andurel:table {{.TableName}}

import (
{{- range .StandardImports}}
	"{{.}}"
{{- end}}
{{if and (gt (len .StandardImports) 0) (gt (len .ExternalImports) 0)}}
{{end}}
{{- range .ExternalImports}}
	"{{.}}"
{{- end}}
)

type {{.NamespaceType}} struct {
	queries *queries.Queries
}

func New{{.NamespaceVar}}(db storage.Connection) {{.NamespaceType}} {
	return {{.NamespaceType}}{queries: queries.New(db)}
}

// WithTx returns a copy that runs queries inside tx.
func ({{.ReceiverName}} {{.NamespaceType}}) WithTx(tx storage.Transaction) {{.NamespaceType}} {
	return {{.NamespaceType}}{queries: queries.New(tx)}
}

type {{.EntityName}} struct {
{{- range .Fields}}
	{{.Name}} {{.Type}} `andurel:"{{.ColumnName}}"`
{{- end}}
}

{{if and .HasPrimaryKey (ne .Mode "create-only")}}
func ({{.ReceiverName}} {{.NamespaceType}}) Find(ctx context.Context, id {{if .IDType}}{{.IDType}}{{else}}uuid.UUID{{end}}) ({{.EntityName}}, error) {
	entity, err := {{.ReceiverName}}.queries.Get{{.EntityName}}[{{.EntityName}}](ctx, id)
	if err != nil {
		if errors.Is(err, pgx.ErrNoRows) {
			return {{.EntityName}}{}, ErrNotFound
		}
		return {{.EntityName}}{}, err
	}

	return entity, nil
}
{{end}}

{{if ne .Mode "read-only"}}
type Create{{.Name}}Data struct {
{{- range .Fields}}
{{- if and (not .IsPrimaryKey) (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
	{{.Name}} {{.Type}}
{{- end}}
{{- end}}
{{- if not .IsAutoIncrementID}}
{{- if and .IDType (ne .IDType "uuid.UUID") (ne .IDType "pgtype.UUID")}}
	{{.IDGoFieldName}} {{.IDType}}
{{- end}}
{{- end}}
}

func ({{.ReceiverName}} {{.NamespaceType}}) Create(ctx context.Context, data Create{{.Name}}Data) ({{.EntityName}}, error) {
	entity := {{.EntityName}}{
{{- if .HasPrimaryKey}}
{{- if not .IsAutoIncrementID}}
{{- if or (not .IDType) (eq .IDType "uuid.UUID") (eq .IDType "pgtype.UUID")}}
		{{.IDGoFieldName}}: uuid.New(),
{{- else}}
		{{.IDGoFieldName}}: data.{{.IDGoFieldName}},
{{- end}}
{{- end}}
{{- end}}
{{- if .HasCreatedAt}}
		CreatedAt: {{if .CreatedAtGoType}}{{.CreatedAtGoType}}{Time: time.Now(), Valid: true}{{else}}time.Now(){{end}},
{{- end}}
{{- if .HasUpdatedAt}}
		UpdatedAt: {{if .UpdatedAtGoType}}{{.UpdatedAtGoType}}{Time: time.Now(), Valid: true}{{else}}time.Now(){{end}},
{{- end}}
{{- range .Fields}}
{{- if and (not .IsPrimaryKey) (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
		{{.Name}}: data.{{.Name}},
{{- end}}
{{- end}}
	}

	if err := validation.Validate(&entity); err != nil {
		return {{.EntityName}}{}, errors.Join(ErrDomainValidation, err)
	}

	return {{.ReceiverName}}.queries.Create{{.EntityName}}[{{.EntityName}}](ctx, queries.Create{{.EntityName}}Params{
{{- range .Fields}}
{{- if and .IsPrimaryKey $.IsAutoIncrementID}}
{{- else}}
		{{.Name}}: entity.{{.Name}},
{{- end}}
{{- end}}
	})
}
{{end}}

{{if and .HasPrimaryKey (eq .Mode "crud")}}
type Update{{.Name}}Data struct {
	{{.IDGoFieldName}} {{if .IDType}}{{.IDType}}{{else}}uuid.UUID{{end}}
{{- range .Fields}}
{{- if and (not .IsPrimaryKey) (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
	{{.Name}} {{.Type}}
{{- end}}
{{- end}}
}

func ({{.ReceiverName}} {{.NamespaceType}}) Update(ctx context.Context, data Update{{.Name}}Data) ({{.EntityName}}, error) {
	entity := {{.EntityName}}{
		{{.IDGoFieldName}}: data.{{.IDGoFieldName}},
{{- if .HasUpdatedAt}}
		UpdatedAt: {{if .UpdatedAtGoType}}{{.UpdatedAtGoType}}{Time: time.Now(), Valid: true}{{else}}time.Now(){{end}},
{{- end}}
{{- range .Fields}}
{{- if and (not .IsPrimaryKey) (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
		{{.Name}}: data.{{.Name}},
{{- end}}
{{- end}}
	}

	if err := validation.Validate(&entity); err != nil {
		return {{.EntityName}}{}, errors.Join(ErrDomainValidation, err)
	}

	row, err := {{.ReceiverName}}.queries.Update{{.EntityName}}[{{.EntityName}}](ctx, queries.Update{{.EntityName}}Params{
		{{.IDGoFieldName}}: entity.{{.IDGoFieldName}},
{{- range .Fields}}
{{- if and (not .IsPrimaryKey) (ne .Name "CreatedAt")}}
		{{.Name}}: entity.{{.Name}},
{{- end}}
{{- end}}
	})
	if err != nil {
		if errors.Is(err, pgx.ErrNoRows) {
			return {{.EntityName}}{}, ErrNotFound
		}
		return {{.EntityName}}{}, err
	}

	return row, nil
}

func ({{.ReceiverName}} {{.NamespaceType}}) Destroy(ctx context.Context, id {{if .IDType}}{{.IDType}}{{else}}uuid.UUID{{end}}) error {
	if err := {{.ReceiverName}}.queries.Delete{{.EntityName}}(ctx, id); err != nil {
		return err
	}

	return nil
}
{{end}}

{{if ne .Mode "create-only"}}
func ({{.ReceiverName}} {{.NamespaceType}}) All(ctx context.Context) ([]{{.EntityName}}, error) {
	return {{.ReceiverName}}.queries.List{{.PluralName}}[{{.EntityName}}](ctx).All()
}

type Paginated{{.PluralName}} struct {
	{{.PluralName}} []{{.EntityName}}
	TotalCount int64
	Page       int64
	PageSize   int64
	TotalPages int64
}

func ({{.ReceiverName}} {{.NamespaceType}}) Paginate(ctx context.Context, page, pageSize int64) (Paginated{{.PluralName}}, error) {
	if page < 1 {
		page = 1
	}
	if pageSize < 1 {
		pageSize = 10
	}
	if pageSize > 100 {
		pageSize = 100
	}

	offset := (page - 1) * pageSize

	totalCount, err := {{.ReceiverName}}.queries.Count{{.PluralName}}(ctx)
	if err != nil {
		return Paginated{{.PluralName}}{}, err
	}

	entities, err := {{.ReceiverName}}.queries.List{{.PluralName}}[{{.EntityName}}](ctx).
		Limit(int32(pageSize)).
		Offset(int32(offset)).
		All()
	if err != nil {
		return Paginated{{.PluralName}}{}, err
	}

	totalPages := (int64(totalCount) + pageSize - 1) / pageSize

	return Paginated{{.PluralName}}{
		{{.PluralName}}:    entities,
		TotalCount: int64(totalCount),
		Page:       page,
		PageSize:   pageSize,
		TotalPages: totalPages,
	}, nil
}
{{end}}

{{if and .HasPrimaryKey (eq .Mode "crud")}}
func ({{.ReceiverName}} {{.NamespaceType}}) Upsert(ctx context.Context, id {{if .IDType}}{{.IDType}}{{else}}uuid.UUID{{end}}, data Create{{.Name}}Data) ({{.EntityName}}, error) {
	entity := {{.EntityName}}{
		{{.IDGoFieldName}}: id,
{{- if .HasCreatedAt}}
		CreatedAt: {{if .CreatedAtGoType}}{{.CreatedAtGoType}}{Time: time.Now(), Valid: true}{{else}}time.Now(){{end}},
{{- end}}
{{- if .HasUpdatedAt}}
		UpdatedAt: {{if .UpdatedAtGoType}}{{.UpdatedAtGoType}}{Time: time.Now(), Valid: true}{{else}}time.Now(){{end}},
{{- end}}
{{- range .Fields}}
{{- if and (not .IsPrimaryKey) (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
		{{.Name}}: data.{{.Name}},
{{- end}}
{{- end}}
	}

	if err := validation.Validate(&entity); err != nil {
		return {{.EntityName}}{}, errors.Join(ErrDomainValidation, err)
	}

	return {{.ReceiverName}}.queries.Upsert{{.EntityName}}[{{.EntityName}}](ctx, queries.Upsert{{.EntityName}}Params{
{{- range .Fields}}
		{{.Name}}: entity.{{.Name}},
{{- end}}
	})
}
{{end}}
