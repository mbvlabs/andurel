package models

import (
{{- range .EntityImports}}
	"{{.}}"
{{- end}}
{{if gt (len .EntityImports) 0}}
{{end}}
	"{{.ModulePath}}/models/internal/db"
)

type {{lower .Name}}Base struct{}

var {{.Name}} = {{lower .Name}}Base{}

type {{.Name}}Entity struct {
{{- range .Fields}}
	{{.Name}} {{.SQLCType}}
{{- end}}
}

func rowTo{{.Name}}Entity(row db.{{.SQLCModelName}}) {{.Name}}Entity {
	return {{.Name}}Entity{
{{- range .Fields}}
		{{.Name}}: row.{{.Name}},
{{- end}}
	}
}
