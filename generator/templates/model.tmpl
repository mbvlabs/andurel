package models
{{if .TableNameOverride}}
// {{.Name | toUpper}}_MODEL_TABLE_NAME: {{.TableNameOverride}}
{{end}}
import (
	"context"
	"errors"
{{- range .StandardImports}}
	"{{.}}"
{{- end}}
{{if and (gt (len .StandardImports) 0) (gt (len .ExternalImports) 0)}}

{{end}}
{{- range .ExternalImports}}
	"{{.}}"
{{- end}}

	"{{.ModulePath}}/models/internal/db"
)

type {{.Name}} struct {
{{- range .Fields}}
	{{.Name}} {{.Type}}
{{- end}}
}

func Find{{.Name}}(
	ctx context.Context,
	dbtx db.DBTX,
	id uuid.UUID,
) ({{.Name}}, error) {
	row, err := queries.Query{{.Name}}ByID(ctx, dbtx, {{uuidParam "id"}})
	if err != nil {
		return {{.Name}}{}, err
	}

{{if hasErrorHandling}}	result, err := rowTo{{.Name}}(row)
	if err != nil {
		return {{.Name}}{}, err
	}
	return result, nil{{else}}	return rowTo{{.Name}}(row), nil{{end}}
}

type Create{{.Name}}Data struct {
{{- range .Fields}}
{{- if and (ne .Name "ID") (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
	{{.Name}} {{.Type}}
{{- end}}
{{- end}}
}

func Create{{.Name}}(
	ctx context.Context,
	dbtx db.DBTX,
	data Create{{.Name}}Data,
) ({{.Name}}, error) {
	if err := validate.Struct(data); err != nil {
		return {{.Name}}{}, errors.Join(ErrDomainValidation, err)
	}

	params := db.BuildInsert{{.Name}}Params(
{{- range .Fields}}
{{- if not (or (eq .Name "ID") (eq .Name "CreatedAt") (eq .Name "UpdatedAt"))}}
		{{.ConversionToDB}},
{{- end}}
{{- end}}
	)
	row, err := queries.Insert{{.Name}}(ctx, dbtx, params)
	if err != nil {
		return {{.Name}}{}, err
	}

{{if hasErrorHandling}}	result, err := rowTo{{.Name}}(row)
	if err != nil {
		return {{.Name}}{}, err
	}
	return result, nil{{else}}	return rowTo{{.Name}}(row), nil{{end}}
}

type Update{{.Name}}Data struct {
	ID uuid.UUID
{{- range .Fields}}
{{- if and (ne .Name "ID") (ne .Name "CreatedAt")}}
	{{.Name}} {{.Type}}
{{- end}}
{{- end}}
}

func Update{{.Name}}(
	ctx context.Context,
	dbtx db.DBTX,
	data Update{{.Name}}Data,
) ({{.Name}}, error) {
	if err := validate.Struct(data); err != nil {
		return {{.Name}}{}, errors.Join(ErrDomainValidation, err)
	}

	currentRow, err := queries.Query{{.Name}}ByID(ctx, dbtx, {{uuidParam "data.ID"}})
	if err != nil {
		return {{.Name}}{}, err
	}

	params := db.BuildUpdate{{.Name}}Params(
		{{if eq .DatabaseType "sqlite"}}data.ID.String(){{else}}data.ID{{end}},
{{- range .Fields}}
{{- if and (ne .Name "ID") (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
		{{.ConversionToDBForUpdate}},
{{- end}}
{{- end}}
	)

	row, err := queries.Update{{.Name}}(ctx, dbtx, params)
	if err != nil {
		return {{.Name}}{}, err
	}

{{if hasErrorHandling}}	result, err := rowTo{{.Name}}(row)
	if err != nil {
		return {{.Name}}{}, err
	}
	return result, nil{{else}}	return rowTo{{.Name}}(row), nil{{end}}
}

func Destroy{{.Name}}(
	ctx context.Context,
	dbtx db.DBTX,
	id uuid.UUID,
) error {
	return queries.Delete{{.Name}}(ctx, dbtx, {{uuidParam "id"}})
}

func All{{.Name}}s(
	ctx context.Context,
	dbtx db.DBTX,
) ([]{{.Name}}, error) {
	rows, err := queries.QueryAll{{.Name}}s(ctx, dbtx)
	if err != nil {
		return nil, err
	}

	{{lower .Name}}s := make([]{{.Name}}, len(rows))
	for i, row := range rows {
{{if hasErrorHandling}}		result, err := rowTo{{.Name}}(row)
		if err != nil {
			return nil, err
		}
		{{lower .Name}}s[i] = result{{else}}		{{lower .Name}}s[i] = rowTo{{.Name}}(row){{end}}
	}

	return {{lower .Name}}s, nil
}

type Paginated{{.Name}}s struct {
	{{.Name}}s []{{.Name}}
	TotalCount int64
	Page       int64
	PageSize   int64
	TotalPages int64
}

func Paginate{{.Name}}s(
	ctx context.Context,
	dbtx db.DBTX,
	page int64,
	pageSize int64,
) (Paginated{{.Name}}s, error) {
	if page < 1 {
		page = 1
	}
	if pageSize < 1 {
		pageSize = 10
	}
	if pageSize > 100 {
		pageSize = 100
	}

	offset := (page - 1) * pageSize

	totalCount, err := queries.Count{{.Name}}s(ctx, dbtx)
	if err != nil {
		return Paginated{{.Name}}s{}, err
	}

	rows, err := queries.QueryPaginated{{.Name}}s(
		ctx,
		dbtx,
		db.BuildQueryPaginated{{.Name}}sParams(pageSize, offset),
	)
	if err != nil {
		return Paginated{{.Name}}s{}, err
	}

	{{lower .Name}}s := make([]{{.Name}}, len(rows))
	for i, row := range rows {
{{if hasErrorHandling}}		result, err := rowTo{{.Name}}(row)
		if err != nil {
			return Paginated{{.Name}}s{}, err
		}
		{{lower .Name}}s[i] = result{{else}}		{{lower .Name}}s[i] = rowTo{{.Name}}(row){{end}}
	}

	totalPages := (totalCount + int64(pageSize) - 1) / int64(pageSize)

	return Paginated{{.Name}}s{
		{{.Name}}s:    {{lower .Name}}s,
		TotalCount: totalCount,
		Page:       page,
		PageSize:   pageSize,
		TotalPages: totalPages,
	}, nil
}

{{if hasErrorHandling}}
func rowTo{{.Name}}(row db.{{SQLCTypeName .TableName}}) ({{.Name}}, error) {
{{- range .Fields}}
{{- if .RequiresErrorHandling}}
	{{lower .Name}}, err := {{.ConversionFromDB}}
	if err != nil {
		return {{$.Name}}{}, err
	}
{{- end}}
{{- end}}

	return {{.Name}}{
{{- range .Fields}}
{{- if .RequiresErrorHandling}}
		{{.Name}}: {{lower .Name}},
{{- else}}
		{{.Name}}: {{.ConversionFromDB}},
{{- end}}
{{- end}}
	}, nil
}
{{else}}
func rowTo{{.Name}}(row db.{{SQLCTypeName .TableName}}) {{.Name}} {
	return {{.Name}}{
{{- range .Fields}}
		{{.Name}}: {{.ConversionFromDB}},
{{- end}}
	}
}
{{end}}

func {{.Name}}Exists(
	ctx context.Context,
	dbtx db.DBTX,
	id uuid.UUID,
) (bool, error) {
	result, err := queries.{{.Name}}Exists(ctx, dbtx, {{uuidParam "id"}})
	if err != nil {
		return false, err
	}
	return result, nil
}

func Upsert{{.Name}}(
	ctx context.Context,
	dbtx db.DBTX,
	data Create{{.Name}}Data,
) ({{.Name}}, error) {
	if err := validate.Struct(data); err != nil {
		return {{.Name}}{}, errors.Join(ErrDomainValidation, err)
	}

	params := db.BuildUpsert{{.Name}}Params(
{{- range .Fields}}
{{- if not (or (eq .Name "ID") (eq .Name "CreatedAt") (eq .Name "UpdatedAt"))}}
		{{.ConversionToDB}},
{{- end}}
{{- end}}
	)
	row, err := queries.Upsert{{.Name}}(ctx, dbtx, params)
	if err != nil {
		return {{.Name}}{}, err
	}

{{if hasErrorHandling}}	result, err := rowTo{{.Name}}(row)
	if err != nil {
		return {{.Name}}{}, err
	}
	return result, nil{{else}}	return rowTo{{.Name}}(row), nil{{end}}
}

type FindOrCreate{{.Name}}Data struct {
{{- range .Fields}}
{{- if and (ne .Name "CreatedAt") (ne .Name "UpdatedAt")}}
	{{.Name}} {{.Type}}
{{- end}}
{{- end}}
}

func FindOrCreate{{.Name}}(
	ctx context.Context,
	dbtx db.DBTX,
	data FindOrCreate{{.Name}}Data,
) ({{.Name}}, bool, error) {
	existing, err := queries.Query{{.Name}}ByID(ctx, dbtx, {{uuidParam "data.ID"}})
	if err == nil {
{{if hasErrorHandling}}		result, convErr := rowTo{{.Name}}(existing)
		if convErr != nil {
			return {{.Name}}{}, false, convErr
		}
		return result, false, nil{{else}}		return rowTo{{.Name}}(existing), false, nil{{end}}
	}

	params := db.BuildInsert{{.Name}}Params(
{{- range .Fields}}
{{- if not (or (eq .Name "ID") (eq .Name "CreatedAt") (eq .Name "UpdatedAt"))}}
		{{.ConversionToDB}},
{{- end}}
{{- end}}
	)
	row, err := queries.Insert{{.Name}}(ctx, dbtx, params)
	if err != nil {
		return {{.Name}}{}, false, err
	}

{{if hasErrorHandling}}	result, err := rowTo{{.Name}}(row)
	if err != nil {
		return {{.Name}}{}, false, err
	}
	return result, true, nil{{else}}	return rowTo{{.Name}}(row), true, nil{{end}}
}

{{range $.PluckableColumns}}
func Find{{$.Name}}By{{.FieldName}}(
	ctx context.Context,
	dbtx db.DBTX,
	{{lowerFirst .FieldName}} {{.GoType}},
) ({{$.Name}}, error) {
	row, err := queries.Find{{$.Name}}By{{.FieldName}}(ctx, dbtx, {{if eq .FieldName "ID"}}{{if eq $.DatabaseType "sqlite"}}{{lowerFirst .FieldName}}.String(){{else}}{{lowerFirst .FieldName}}{{end}}{{else}}{{lowerFirst .FieldName}}{{end}})
	if err != nil {
		return {{$.Name}}{}, err
	}

{{if hasErrorHandling}}	result, err := rowTo{{$.Name}}(row)
	if err != nil {
		return {{$.Name}}{}, err
	}
	return result, nil{{else}}	return rowTo{{$.Name}}(row), nil{{end}}
}
{{end}}

{{range $.PluckableColumns}}
func Pluck{{$.Name}}{{.FieldName}}s(
	ctx context.Context,
	dbtx db.DBTX,
) ([]{{.GoType}}, error) {
	rows, err := queries.Pluck{{$.Name}}{{.FieldName}}s(ctx, dbtx)
	if err != nil {
		return nil, err
	}

{{if eq .FieldName "ID"}}{{if eq $.DatabaseType "sqlite"}}	result := make([]{{.GoType}}, len(rows))
	for i, row := range rows {
		id, err := uuid.Parse(row)
		if err != nil {
			return nil, err
		}
		result[i] = id
	}
	return result, nil{{else}}	return rows, nil{{end}}{{else}}	return rows, nil{{end}}
}
{{end}}
